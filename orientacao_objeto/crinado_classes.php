<?php

/*
    Vamos supor que eu tenho o sistema de um banco. Para que eu seja um cliente no banco eu preciso criar uma conta. Porém, essa conta tem um modelo de criação que deve ser 
    seguido por todos, chamado classe. Para a definição dessa classe eu uso a palavra reservada "class" seguida do nome dessa classe contendo a primeira letra de cada palavra
    desse nome em uppercase. É dentro dessa classe que definimos os dados da conta, chamados atributos, e as funções, chamadas métodos. Agora, para realmente criar um novo 
    objeto, ou seja, uma variável que tenha os comportamentos da classe conta, eu preciso utilizar a seguinte sintaxe: new <classe>(). 
*/

class Conta{
    //definir dados da conta
    
}

$novaConta = new Conta();